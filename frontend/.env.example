# ========================================
# ChocoChoco Frontend Environment Config
# ========================================

# ┌─────────────────────────────────────┐
# │  SOLANA (PRIMARY BLOCKCHAIN)        │
# └─────────────────────────────────────┘

# Solana cluster to connect to
# Options: devnet | testnet | mainnet-beta
VITE_SOLANA_CLUSTER=devnet

# Your deployed Anchor program ID (public key)
# Get this after deploying with `anchor deploy` or from Anchor.toml
# Example deployed program on devnet:
VITE_PROGRAM_ID=J5GgxY8zobKvjJovnENncHDLWVQ2gBPH2skhTKL8JuGz

# Default stake amount in lamports (0.001 SOL = 1,000,000 lamports)
VITE_STAKE_LAMPORTS=1000000

# Custom RPC endpoint (optional - leave empty to use default cluster RPC)
# Example: https://api.devnet.solana.com
# Example: https://solana-devnet.g.alchemy.com/v2/YOUR-API-KEY
VITE_SOLANA_RPC_URL=https://api.devnet.solana.com

# ┌─────────────────────────────────────┐
# │  EVM CHAINS (LEGACY/OPTIONAL)       │
# └─────────────────────────────────────┘

# EVM Network (Base Sepolia = 84532; Polygon Mumbai = 80001; Amoy = 80002)
VITE_CHAIN_ID=84532

# EVM RPC URL for the selected network
VITE_RPC_URL=https://sepolia.base.org

# EVM Deployed contract addresses
VITE_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
VITE_TREASURY_ADDRESS=0x0000000000000000000000000000000000000000

# Back-compat (optional)
VITE_GAME_ADDRESS=0x0000000000000000000000000000000000000000

# Additional EVM RPC URLs
VITE_BASE_SEPOLIA_RPC=https://sepolia.base.org
VITE_POLYGON_MUMBAI_RPC=https://rpc.ankr.com/polygon_mumbai

# ┌─────────────────────────────────────┐
# │  WALLET & WEB3 INFRASTRUCTURE       │
# └─────────────────────────────────────┘

# WalletConnect v2 project ID (required for EVM wallet modal)
# Get yours at: https://cloud.walletconnect.com/
VITE_WALLETCONNECT_PROJECT_ID=J5GgxY8zobKvjJovnENncHDLWVQ2gBPH2skhTKL8JuGz

# ┌─────────────────────────────────────┐
# │  SUBGRAPH & INDEXING                │
# └─────────────────────────────────────┘

# Solana Indexer REST API endpoint
# This is the local/deployed indexer service that provides leaderboard data
# Local development: http://localhost:3001
# Production: https://your-indexer-url.com
VITE_INDEXER_URL=http://localhost:3001

# The Graph Studio endpoint for EVM indexing (legacy)
VITE_SUBGRAPH_URL=

# Optional: Helius API key for enhanced Solana RPC/indexing
VITE_HELIUS_API_KEY=

# ========================================
# NOTES:
# ========================================
# 
# For Solana deployment:
#   1. Deploy program: `anchor deploy` (in contracts/ dir)
#   2. Copy program ID to VITE_PROGRAM_ID
#   3. Update Anchor.toml with program ID
#   4. Restart frontend dev server
#
# For EVM deployment:
#   1. Run: `cd contracts && forge script script/Deploy.s.sol --broadcast`
#   2. Copy contract address to VITE_CONTRACT_ADDRESS
#   3. Update scripts/.env if using deployment scripts
#
# Scripts automatically use `solana config get` for cluster/keypair.
# No need to duplicate RPC URLs in scripts.

